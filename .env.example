# 复制本文件为 .env，再填入你自己的 Key：
#   Windows:  copy .env.example .env
#   macOS/Linux:  cp .env.example .env
#
# 本项目默认调用 DeepSeek 的 OpenAI 兼容接口，
# 因此需要的是 DeepSeek 的 Key（见 core/llm_clients.py 的 base_url）。
API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxx
